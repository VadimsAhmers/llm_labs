# Мультиагентный помощник по учёбе

Система из нескольких специализированных агентов для помощи с задачами по программированию, теории и планированию учёбы.

## Содержание

- [Архитектура системы](#архитектура-системы)
- [Установка и запуск](#установка-и-запуск)
- [Структура проекта](#структура-проекта)
- [Использование](#использование)
- [Эксперименты](#эксперименты)
- [Рефлексия](#рефлексия)

---

## Архитектура системы

Система реализует **паттерн Router** с тремя специализированными агентами:

```
User Query
    ↓
[Router Agent] ← классификация запроса
    ↓
    ├─→ [Theory Agent] → теоретические объяснения
    ├─→ [Code Helper Agent] → генерация и отладка кода
    └─→ [Planner Agent] → создание учебных планов
    ↓
Final Response
```

### Агенты

1. **Router Agent** — классифицирует запрос (`theory`/`coding`/`planning`)
2. **Theory Agent** — объясняет концепции с ключевыми терминами
3. **Code Helper Agent** — генерирует код, тесты, выполняет проверку
4. **Planner Agent** — создаёт структурированные учебные планы

### Инструменты (Tools)

- `search_notes` — поиск в базе знаний
- `execute_code` — выполнение сгенерированного кода
- `save_plan` — сохранение планов в JSON

### Память

- **In-memory история** — хранит диалог в рамках сессии
- **Студенческий профиль** — персистентное хранилище изученных тем

Подробнее: [ARCHITECTURE.md](ARCHITECTURE.md)

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/VadimsAhmers/llm_labs/lab_2
cd lab_2
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Зависимости:**
```
langchain==0.3.20
langchain-openai==0.3.2
langgraph==0.2.77
pydantic==2.10.6
python-dotenv==1.0.1
```

### 3. Настройка окружения

Создайте файл `.env` в корне проекта:

```env
UNIVERSITY_API_BASE=http://your-vllm-server:8000/v1
UNIVERSITY_API_KEY=EMPTY
LLM_MODEL=qwen3-32b
```

### 4. Запуск системы

**Вариант 1: Jupyter Notebook (рекомендуется)**

```bash
jupyter notebook lab_2.ipynb
```

Выполните ячейки последовательно:
1. Инициализация (загрузка модели)
2. Базовые примеры
3. Эксперименты (5+ запросов)
4. Анализ результатов

**Вариант 2: Python-скрипт**

```python
from langchain_openai import ChatOpenAI
from src import MultiAgentGraph, MemoryManager

llm = ChatOpenAI(base_url="...", api_key="...", model="qwen3-32b")
memory = MemoryManager()
graph = MultiAgentGraph(llm=llm, memory=memory)

response = graph.invoke("Объясни паттерн supervisor в МАС")
print(response)
```

---

## 📂 Структура проекта

```
lab_2/
├── src/
│   ├── __init__.py          # Экспорты для пакета
│   ├── graph.py             # LangGraph-граф системы
│   ├── agents.py            # Реализация всех агентов
│   ├── memory.py            # Управление памятью
│   ├── models.py            # Pydantic-модели
│   └── tools.py             # Tool calling (инструменты)
│
├── data/
│   ├── student_profile.json # Профиль пользователя (автосоздаётся)
│   └── study_plans.json     # Сохранённые планы
│
├── lab_2.ipynb              # Основной ноутбук с экспериментами
├── requirements.txt         # Зависимости
├── .env                     # Конфигурация (создайте сами)
│
├── ARCHITECTURE.md          # Детальная архитектура
├── REFLECTION.md            # Анализ и рефлексия
└── README.md                # Этот файл
```

---

## 💡 Использование

### Примеры запросов

**Теоретический вопрос:**
```python
graph.invoke("Объясни паттерн supervisor в мультиагентных системах")
```

**Задача на программирование:**
```python
graph.invoke("Напиши функцию для проверки корректности скобочной последовательности с тестами")
```

**Создание учебного плана:**
```python
graph.invoke("Составь план подготовки к экзамену по Computer Vision на 3 недели")
```

**Комбинированный запрос:**
```python
graph.invoke("Объясни алгоритм Дейкстры и покажи его реализацию на Python")
```

### Просмотр истории

```python
# История диалога
for entry in memory.get_recent_history():
    print(f"[{entry.role}] {entry.agent}: {entry.content[:50]}...")

# Профиль студента
print(memory.profile.studied_topics)
```

---

## 🧪 Эксперименты

В ноутбуке `lab_2.ipynb` реализовано **5 экспериментов** разных категорий:

1. **Концептуальный вопрос** — паттерны supervisor vs router
2. **Проектирование** — архитектура микросервисной системы
3. **Программирование** — проверка скобочной последовательности
4. **Планирование** — подготовка к экзамену
5. **Комбинированный запрос** — теория + код (алгоритм Дейкстры)

Для каждого эксперимента анализируется:
- Какие агенты активированы
- Использование инструментов
- Работа с памятью
- Качество ответа

**Запуск экспериментов:**

```bash
jupyter notebook lab_2.ipynb
# Выполните ячейки в разделе "Эксперименты и оценка системы"
```

---

## 🔍 Рефлексия

Подробный анализ системы доступен в [REFLECTION.md](REFLECTION.md).

### Ключевые выводы

**✅ Что работает хорошо:**
- Router-паттерн обеспечивает точную маршрутизацию
- Специализированные агенты дают качественные ответы
- Tool calling работает осмысленно
- Память сохраняет контекст между запросами

**⚠️ Ограничения:**
- Комбинированные запросы обрабатываются неполностью
- Отсутствие контроля качества (supervisor)
- Упрощённая память (нет semantic search)
- Tool-заглушки (`execute_code` не выполняет код реально)

**🚀 Идеи для развития:**
- Добавить Supervisor Agent для проверки качества
- Реализовать sequential workflow для multi-step задач
- Внедрить vector database (ChromaDB) для RAG
- Интегрировать реальное выполнение кода (E2B/Docker)

---

## Технологии

- **LangGraph** — оркестрация мультиагентного графа
- **LangChain** — интеграция с LLM
- **Qwen 3 (32B)** — языковая модель через vLLM
- **Pydantic** — структурированный вывод и валидация
- **Python 3.12+** — основной язык

---

## Дополнительная документация

- [ARCHITECTURE.md](ARCHITECTURE.md) — детальный дизайн системы
- [REFLECTION.md](REFLECTION.md) — анализ работы и идеи улучшений
- [lab_2.md](lab_2.md) — задание лабораторной работы

---

## Лицензия

Учебный проект для курса по LLM и мультиагентным системам.
